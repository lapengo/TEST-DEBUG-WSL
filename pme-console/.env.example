# PME SOAP Service Configuration
# Copy this file to .env and update with your actual credentials

# Endpoint URL (optional - defaults to http://beitvmpme01.beitm.id/EWS/DataExchange.svc)
PME_ENDPOINT_URL=http://beitvmpme01.beitm.id/EWS/DataExchange.svc

# Authentication credentials (required)
# Replace these with your actual credentials
PME_USERNAME=supervisor
PME_PASSWORD=P@ssw0rdpme

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials (if different from above)
# 3. Run the application: cd pme-console/PME/PME && dotnet run
#
# The application will automatically load variables from the .env file!
# No need to manually export/set environment variables.
#
# Note: The .env file is excluded from git via .gitignore for security.

