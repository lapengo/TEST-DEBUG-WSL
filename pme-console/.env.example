# PME SOAP Service Configuration Example
# Copy this file to .env and update with your actual credentials

# Endpoint URL (optional - defaults to http://beitvmpme01.beitm.id/EWS/DataExchange.svc)
# PME_ENDPOINT_URL=http://your-pme-server.com/EWS/DataExchange.svc

# Authentication credentials (required)
# Replace these with your actual credentials
PME_USERNAME=your-username
PME_PASSWORD=your-password

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. Linux/macOS: source .env
#    Then run: cd pme-console/PME && dotnet run --project PME.csproj
#
# 4. Windows PowerShell:
#    $env:PME_USERNAME="your-username"
#    $env:PME_PASSWORD="your-password"
#    cd pme-console/PME
#    dotnet run --project PME.csproj

